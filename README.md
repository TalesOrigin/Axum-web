# Axum-web
create main administrator for managing other sellers and also the overal key generator
create resellers panel so each reseller can login and request for keys to be granted by main admin( resellers have full management over keys assigned to them )
creta verification system that verifies the keys using the unique device id  then cheks and tracks the expiry as well as the limmit of devices that can use that key at that time simultaneously to avoid over using that key for different deices
secure the data trasport to and from the api 

web for key panels,including sellers panel,main admin panel and licensing system and verification and key generation
